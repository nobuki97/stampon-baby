name: Deploy baby → stampon-for-baby

on:
  push:
    branches:
      - baby

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create Vercel project link
        run: |
          mkdir -p .vercel
          echo '{"projectId":"prj_U76X02CsVBD32tHrGg74rjY5rccp","orgId":"team_Y4hn81HC3O9Qd5BGqxqHMJeS"}' > .vercel/project.json

      - name: Deploy to Vercel (stampon-for-baby)
        id: deploy
        run: |
          DEPLOY_URL=$(npx vercel deploy --yes --token=${{ secrets.VERCEL_TOKEN }} 2>&1 | grep -o 'https://[^ ]*vercel\.app')
          echo "deploy_url=$DEPLOY_URL" >> $GITHUB_OUTPUT
          echo "Deployed to: $DEPLOY_URL"

      - name: Assign production alias
        run: |
          npx vercel alias set ${{ steps.deploy.outputs.deploy_url }} stampon-for-baby.vercel.app --token=${{ secrets.VERCEL_TOKEN }} --scope=nobuki-higashitanis-projects
          echo "Production alias set: stampon-for-baby.vercel.app → ${{ steps.deploy.outputs.deploy_url }}"
